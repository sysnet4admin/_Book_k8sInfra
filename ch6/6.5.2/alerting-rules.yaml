serverFiles:
  alerting_rules.yml:
    groups:
    - name: Node
      rules:
      - alert: NodeDown
        expr: avg(up{job="kubernetes-nodes"}) < 1
        for: 15s
        annotations:
          description: 'kubernetes node down'