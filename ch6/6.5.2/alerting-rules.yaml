serverFiles:
  alerting_rules.yml:
    groups:
    - name: Nodes
      rules:
      - alert: Nodes
        expr: avg(up{job="kubernetes-nodes"}) < 1
        for: 15s
        annotations:
          message: 'kubernetes node down'